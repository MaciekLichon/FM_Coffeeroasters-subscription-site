import './CollectionTitle.scss';

const CollectionTitle = () => {
    return (
        <div className="collection__title">
            <p className="accent">our collection</p>
        </div>
    );
};

export default CollectionTitle;
